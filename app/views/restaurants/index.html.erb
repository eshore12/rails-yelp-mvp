<div class="container">
  <h1> 🍖 List of All Restaurants 🍖 </h1>
    <div class="text-center">
      <button type="button" class="btn btn-outline-primary"> <%= link_to "+ Add a new restaurant", new_restaurant_path, class: 'btn-title' %></button>
    </div>

    <div class="cards">
    <% @restaurants.each do |restaurant| %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://source.unsplash.com/collection/345760/1600x900")>
        <h2><%= link_to restaurant.name, restaurant_path(restaurant), class: 'title-link' %></h2>
      </div>
    <% end %>
    </div>
</div>
